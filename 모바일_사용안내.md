# 모바일 연결 및 오프라인 사용

## PC 서버 시작

1. `장비대여관리 실행.bat`을 실행합니다.
2. Windows 방화벽 창이 나타나면 **개인 네트워크** 접근을 허용합니다.
3. 열린 웹 화면의 `동기화` 메뉴에서 모바일 접속 주소를 확인합니다.
4. 같은 화면의 6자리 `모바일 연결 PIN`을 휴대폰에 입력합니다.
5. 서버 콘솔 창을 닫으면 모바일 접속과 동기화가 중단됩니다.

## 같은 Wi-Fi에서 연결

1. PC와 안드로이드를 같은 Wi-Fi에 연결합니다.
2. 안드로이드 Chrome에서 PC 화면에 표시된 `http://PC주소:8765`를 엽니다.
3. 인터넷 연결은 필요 없지만 PC 서버는 켜져 있어야 합니다.

일반 HTTP로 접속한 Wi-Fi 화면은 연결이 끊긴 뒤 **열려 있는 화면에서** 대여·반납을 계속 기록할 수 있습니다. 브라우저를 완전히 종료한 뒤에도 앱을 새로 열어야 한다면 HTTPS 인증서 설정이 필요합니다. `ERM_CERT_FILE`, `ERM_KEY_FILE` 환경 변수에 안드로이드가 신뢰하는 인증서와 키 경로를 지정한 뒤 서버를 실행하세요.

## USB 케이블로 직접 연결

USB 방식은 Android 개발자 도구의 포트 연결을 사용합니다.

1. 안드로이드에서 `개발자 옵션 > USB 디버깅`을 켭니다.
2. PC에 Android Platform Tools를 설치하고 `adb`를 PATH에 추가합니다.
3. PC 서버를 먼저 실행합니다.
4. USB 케이블을 연결하고 휴대폰의 PC 허용 메시지를 승인합니다.
5. `모바일_USB_연결.bat`을 실행합니다.

휴대폰에서는 `http://localhost:8765`가 열립니다. localhost 접속은 서비스 워커를 사용할 수 있으므로 Chrome 메뉴에서 **앱 설치** 또는 **홈 화면에 추가**를 선택하면 PC 연결 없이 앱 화면을 다시 열고 오프라인 작업을 저장할 수 있습니다.

Wi-Fi 주소와 USB의 `localhost` 주소는 브라우저에서 서로 다른 앱 저장 공간으로 취급됩니다. 오프라인 작업을 저장한 뒤에는 해당 작업을 동기화하기 전까지 같은 연결 주소를 사용하세요. 안정적인 오프라인 운용은 USB에서 설치한 `localhost` 앱을 기본으로 사용하는 것을 권장합니다.

## 동기화 규칙

- 연결이 없을 때 대여·반납은 휴대폰 저장소의 `전송 대기`에 보관됩니다.
- 서버에 다시 연결하면 입력 순서대로 자동 전송됩니다.
- 같은 장비의 마지막 수량을 다른 기기가 먼저 대여하면 해당 작업은 `충돌`로 표시됩니다.
- 충돌 작업은 중앙 DB에 반영되지 않습니다. 내용을 확인한 뒤 충돌 기록을 삭제하세요.
- 동기화 전에는 Chrome 사이트 데이터나 앱 데이터를 삭제하지 마세요.
- 장비·대여자 관리, 통계 조정, 파일 출력은 서버 연결 상태에서만 사용할 수 있습니다.
